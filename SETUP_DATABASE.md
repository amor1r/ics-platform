# إعداد قاعدة البيانات والوصول للواجهات

## 🎯 الهدف
الوصول إلى لوحات التحكم والواجهات المحمية بعد تسجيل الدخول.

---

## ⚡ الحل السريع (3 خطوات)

### الخطوة 1: تشغيل PostgreSQL

```bash
# على Kali Linux
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

### الخطوة 2: إنشاء قاعدة البيانات

```bash
sudo -u postgres psql << 'EOF'
CREATE DATABASE ics_platform;
CREATE USER ics_user WITH PASSWORD 'ics_password';
ALTER USER ics_user CREATEDB;
GRANT ALL PRIVILEGES ON DATABASE ics_platform TO ics_user;
\q
EOF
```

### الخطوة 3: إعداد Prisma وإنشاء الحسابات

```bash
cd "/home/kali/Desktop/New Folder"

# إنشاء الجداول
npx prisma db push

# إنشاء الحسابات التجريبية
npm run db:seed
```

---

## ✅ بعد الإعداد

### حساب المدير:
- **URL**: http://localhost:3000/login/admin
- **Email**: `admin@ics.com`
- **Password**: `Admin123!@#Password`
- **سيتم توجيهك إلى**: `/admin/dashboard`

### حساب العضو:
- **URL**: http://localhost:3000/login/member
- **Email**: `member@ics.com`
- **Password**: `Member123!@#Password`
- **سيتم توجيهك إلى**: `/member/dashboard`

---

## 🎨 ما ستراه بعد تسجيل الدخول

### لوحة تحكم المدير (`/admin/dashboard`):
- ✅ ترحيب باسمك
- ✅ إحصائيات شاملة (المستخدمين، المشاريع، التعليقات، السجلات)
- ✅ بطاقات ملونة مع أرقام
- ✅ أزرار إجراءات سريعة
- ✅ Sidebar مع قائمة تنقل كاملة

### لوحة تحكم العضو (`/member/dashboard`):
- ✅ ترحيب باسمك
- ✅ إحصائيات (المشاريع، الإعجابات، التعليقات)
- ✅ بطاقات ملونة
- ✅ أزرار إجراءات سريعة
- ✅ Sidebar مع قائمة تنقل

---

## 🔍 التحقق من الإعداد

```bash
# التحقق من اتصال قاعدة البيانات
cd "/home/kali/Desktop/New Folder"
node test-db-connection.js

# يجب أن ترى: ✅ Database connection successful!
```

---

## 🐛 حل المشاكل

### المشكلة: "Can't reach database server"
**الحل**:
```bash
# التحقق من حالة PostgreSQL
sudo systemctl status postgresql

# إذا لم يكن يعمل:
sudo systemctl start postgresql
```

### المشكلة: "database does not exist"
**الحل**:
```bash
sudo -u postgres psql -c "CREATE DATABASE ics_platform;"
```

### المشكلة: "permission denied"
**الحل**:
```bash
sudo -u postgres psql << 'EOF'
GRANT ALL PRIVILEGES ON DATABASE ics_platform TO ics_user;
\q
EOF
```

---

## 📝 ملاحظات

1. **أول مرة**: بعد `npx prisma db push`، سيتم إنشاء جميع الجداول
2. **البيانات**: `npm run db:seed` سينشئ الحسابات التجريبية + مشروع تجريبي
3. **Prisma Studio**: يمكنك عرض البيانات بـ `npx prisma studio`

---

## 🚀 بعد تسجيل الدخول

ستتمكن من:
- ✅ رؤية لوحة التحكم
- ✅ استكشاف جميع الصفحات
- ✅ إنشاء مشاريع جديدة (للمدراء)
- ✅ إدارة المستخدمين (للمدراء)
- ✅ عرض السجلات (للمدراء)
- ✅ استكشاف المشاريع (للأعضاء)
- ✅ تعديل الملف الشخصي (للأعضاء)

---

**جاهز للبدء!** 🎉

