# دليل البدء السريع - منصة ICS

## 🎯 الوضع الحالي

✅ **الموقع يعمل**: http://localhost:3000  
⚠️ **قاعدة البيانات**: غير متاحة حالياً  
✅ **جميع الصفحات**: موجودة ومصممة بشكل صحيح

---

## 📍 الواجهات المتاحة

### 🌐 الصفحات العامة (تعمل الآن)
- ✅ `/` - الصفحة الرئيسية
- ✅ `/about` - صفحة من نحن
- ✅ `/login` - صفحة تسجيل الدخول
- ✅ `/login/member` - تسجيل دخول الأعضاء
- ✅ `/login/admin` - تسجيل دخول المدراء
- ✅ `/register` - إنشاء حساب جديد

### 🔒 الصفحات المحمية (تحتاج تسجيل دخول)

#### لوحة تحكم الأعضاء:
- 📍 `/member/dashboard` - لوحة التحكم الرئيسية
- 📍 `/member/profile` - الملف الشخصي
- 📍 `/member/projects` - قائمة المشاريع
- 📍 `/member/project/[id]` - صفحة المشروع

#### لوحة تحكم المدراء:
- 📍 `/admin/dashboard` - لوحة التحكم الرئيسية
- 📍 `/admin/projects` - إدارة المشاريع
- 📍 `/admin/projects/new` - إنشاء مشروع جديد
- 📍 `/admin/projects/[id]/edit` - تعديل مشروع
- 📍 `/admin/users` - إدارة المستخدمين
- 📍 `/admin/logs` - سجلات النظام
- 📍 `/admin/invites` - دعوات المدراء
- 📍 `/admin/settings` - الإعدادات

---

## 🚀 كيفية الوصول للواجهات المحمية

### الخطوة 1: إعداد قاعدة البيانات

```bash
# تشغيل PostgreSQL
sudo systemctl start postgresql

# إنشاء قاعدة البيانات
sudo -u postgres psql << EOF
CREATE DATABASE ics_platform;
CREATE USER ics_user WITH PASSWORD 'ics_password';
GRANT ALL PRIVILEGES ON DATABASE ics_platform TO ics_user;
\q
EOF

# إعداد Prisma
cd "/home/kali/Desktop/New Folder"
npx prisma db push
```

### الخطوة 2: إنشاء حساب مدير

```bash
# إنشاء حساب المدير
npx tsx scripts/create-admin.ts
```

**الحساب:**
- 📧 Email: `admin@ics.com`
- 🔑 Password: `Admin123!@#Password`

### الخطوة 3: تسجيل الدخول

1. اذهب إلى: http://localhost:3000/login/admin
2. أدخل بيانات المدير
3. سيتم توجيهك تلقائياً إلى: `/admin/dashboard`

---

## 🎨 معاينة الواجهات (بدون قاعدة بيانات)

يمكنك معاينة كود الواجهات في:

### لوحة تحكم الأعضاء:
- `app/(dashboard)/member/dashboard/page.tsx`
- `app/(dashboard)/member/profile/page.tsx`
- `app/(dashboard)/member/projects/page.tsx`
- `app/(dashboard)/member/project/[id]/page.tsx`

### لوحة تحكم المدراء:
- `app/(dashboard)/admin/dashboard/page.tsx`
- `app/(dashboard)/admin/projects/page.tsx`
- `app/(dashboard)/admin/users/page.tsx`
- `app/(dashboard)/admin/logs/page.tsx`
- `app/(dashboard)/admin/invites/page.tsx`
- `app/(dashboard)/admin/settings/page.tsx`

---

## 📸 ماذا تحتوي الواجهات؟

### لوحة تحكم الأعضاء (`/member/dashboard`):
- ✅ ترحيب باسم المستخدم
- ✅ إحصائيات (عدد المشاريع، الإعجابات، التعليقات)
- ✅ بطاقات إحصائيات مع ألوان مختلفة
- ✅ أزرار إجراءات سريعة
- ✅ Sidebar مع قائمة التنقل

### لوحة تحكم المدراء (`/admin/dashboard`):
- ✅ ترحيب باسم المدير
- ✅ إحصائيات شاملة (المستخدمين، المشاريع، التعليقات، السجلات)
- ✅ بطاقات إحصائيات مفصلة
- ✅ أزرار إجراءات سريعة
- ✅ Sidebar مع قائمة تنقل كاملة

### إدارة المشاريع (`/admin/projects`):
- ✅ قائمة جميع المشاريع
- ✅ بحث وفلترة
- ✅ أزرار تعديل وحذف
- ✅ زر إنشاء مشروع جديد

### إدارة المستخدمين (`/admin/users`):
- ✅ قائمة جميع المستخدمين
- ✅ بحث عن المستخدمين
- ✅ أزرار حظر/إلغاء حظر
- ✅ عرض معلومات المستخدمين

---

## 🔧 حل مشكلة قاعدة البيانات

إذا لم تستطع تشغيل PostgreSQL، يمكنك:

### خيار 1: استخدام SQLite (مؤقت)
```bash
# تعديل prisma/schema.prisma
# تغيير:
# datasource db {
#   provider = "postgresql"
#   url      = env("DATABASE_URL")
# }
# إلى:
# datasource db {
#   provider = "sqlite"
#   url      = "file:./dev.db"
# }

# ثم:
npx prisma db push
npx tsx scripts/create-admin.ts
```

### خيار 2: استخدام Docker
```bash
docker run --name postgres-ics \
  -e POSTGRES_PASSWORD=ics_password \
  -e POSTGRES_USER=ics_user \
  -e POSTGRES_DB=ics_platform \
  -p 5432:5432 \
  -d postgres:15
```

---

## ✅ الخطوات التالية

1. ✅ إعداد قاعدة البيانات
2. ✅ إنشاء حساب مدير
3. ✅ تسجيل الدخول
4. ✅ استكشاف جميع الواجهات
5. ✅ إنشاء مشروع تجريبي
6. ✅ اختبار جميع الميزات

---

**ملاحظة**: جميع الواجهات موجودة ومصممة بشكل صحيح. فقط تحتاج قاعدة البيانات للعمل!
